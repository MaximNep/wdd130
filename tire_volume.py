# from math import *
# w = float(input('enter the width: '))
# a = float(input('enter the aspect ratio of the tire: '))
# d = float(input('enter the diameter of the wheel: '))
# v = (pi * w ** 2 * a * (w * a + 2540 * d)) / (10 ** 10)
# print(v)

print(3 * 2 - 17 * 2 * 2 / (2 + 5))
print(3 * 2 - (17 * 2 * 2) / 2 + 5)